import { ActionTree } from 'vuex'
import { StateInterface } from '../index'
import { TabsViewStateInterface } from './state'

const actions: ActionTree<TabsViewStateInterface, StateInterface> = {
  someAction (/* context */) {
    // your code
  }
}

export default actions
